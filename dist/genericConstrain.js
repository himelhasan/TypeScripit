"use strict";
// Generic Constrain learning file
const addMeInMyCrushMind = (param) => {
    const crush = "Kate  Wing let";
    return Object.assign(Object.assign({}, param), { crush });
};
const myPerInfo = {
    name: "Himel",
    age: 100,
    salary: 300000,
    isFreelancer: true,
};
const crushHeart = addMeInMyCrushMind(myPerInfo);
